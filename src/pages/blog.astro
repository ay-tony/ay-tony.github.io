---
import { getCollection } from "astro:content"
import BaseLayout from "/src/layouts/base-layout.astro"
import PostPreview from "/src/components/post-preview.astro"

const blogs_typst = await getCollection("typ")
const pageTitle = "文章";
---

<BaseLayout pageTitle={pageTitle}>
    {blogs_typst.map((post: any) => <PostPreview post={post} url=`/posts/${post.id}/`/>)}
</BaseLayout>
